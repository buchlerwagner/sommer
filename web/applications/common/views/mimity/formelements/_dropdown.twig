{% import "formBuilderMacros.twig" as self %}
TO BE IMPLEMENT!!!
<a {% if button.id is not empty %}id="{{ button.getId }}"{% endif %} class="{{ button.getClass }}{% if button.isDisabled %} disabled{% endif %}" href="{{ button.getUrl }}"{% if button.getTarget %} target="{{ button.getTarget }}"{% endif %}{{ self.attributes(element.getAttributes) }}>
    {{ self.icon(element) }}
    {{ _(button.getCaption) }}
</a>
